import Container from '../../common/Container'
import SectionTitle from '../../common/SectionTitle'
import './Classes.css'

const Classes = () => (
  <section className="sf-classes">
    <Container>
      <SectionTitle title="Classes that move with you" description="From foundational strength to high-intensity conditioning, every class is coached and scalable." />
    </Container>
  </section>
)

export default Classes
